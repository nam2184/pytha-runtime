{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "pytha-runtime": {
      "type": "local",
      "command": ["node.exe", "--import", "tsx", "runtime/mcp.ts"],
      "enabled": true
    }
  }
}
